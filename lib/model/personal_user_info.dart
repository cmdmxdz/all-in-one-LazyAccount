class UserInfo {
  int index;

  /// icon 图片路径
  String iconPath;

  /// 设置名称
  String name;

  /// 设置信息
  String infoContent;

  UserInfo({this.index, this.iconPath, this.name, this.infoContent});
}
